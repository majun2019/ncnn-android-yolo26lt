# ── Paper drafts (keep off public repo during review) ────────────────────────
Paper.md
PaperEnglish.md
back/
Paper_backup_captions.md

# ── Vale linting config (local writing tool, not project code) ───────────────
.vale/
.vale.ini
Alignment.md
.markdownlint.jsonc
cspell.json

# ── Local test notes (not project code) ──────────────────────────────────────
test/

# ── Android build outputs ─────────────────────────────────────────────────────
/build/
app/build/
app/.cxx/

# ── Gradle ────────────────────────────────────────────────────────────────────
.gradle/
gradle/wrapper/gradle-wrapper.jar

# ── Android local config (machine-specific) ───────────────────────────────────
# local.properties — 已允许上传，不再忽略

# ── IDE ───────────────────────────────────────────────────────────────────────
.idea/
*.iml

# ── Python virtualenv ─────────────────────────────────────────────────────────
.venv/
__pycache__/
*.pyc

# ── modelback/ — local backup dir, redundant with assets; excluded entirely ───
modelback/

# ── Training datasets (large, reference Roboflow source instead) ──────────────
data/train/
data/valid/
data/hardneg_pool/

# ── Training / experiment run outputs ─────────────────────────────────────────
runs/

# ── Backup files ──────────────────────────────────────────────────────────────
*.bak

# ── OS ────────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Logcat dumps (runtime, not source) ───────────────────────────────────────
# logcat.txt — 已允许上传，不再忽略

# ── Large video files (exceed GitHub 100 MB limit) ───────────────────────────
*.mp4
*.avi
*.mov

# ── Root-level model weights (explicitly allowed) ────────────────────────────
!/yolo26n.pt
!/best.pt

# ── scripts/ — paper/figure/bench/one-off scripts (not project runtime) ──────
# Paper figure generation
scripts/build_figure8_ab.py
scripts/build_figure8_combined.py
scripts/build_figure8_montage.py
scripts/build_paper_figure_assets.py
scripts/_patch_captions.py
scripts/_patch_contributions.py

# Benchmarking / latency collection (paper results)
scripts/benchmark_yolo26.py
scripts/collect_latency.py
scripts/trace_conversion_pipeline.py

# One-time C++ patch scripts (already applied)
scripts/_fix_encoding.py
scripts/_fix_threshold.py
scripts/_fix_topk.py

# Miscellaneous one-off utilities
scripts/_quick_asset_stats.py
